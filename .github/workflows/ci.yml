name: ci
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: '8.0.x' }
      - run: dotnet restore RealTimeHelpdesk.slnx
      - run: dotnet build RealTimeHelpdesk.slnx --no-restore -c Release
      - run: dotnet test tests/Helpdesk.UnitTests/Helpdesk.UnitTests.csproj --no-build -c Release
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: 'npm', cache-dependency-path: frontend/helpdesk-web/package-lock.json }
      - run: npm ci
        working-directory: frontend/helpdesk-web
      - run: npm run build
        working-directory: frontend/helpdesk-web
      - run: ls ../src/Helpdesk.API/wwwroot/index.html
  integrated:
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: '8.0.x' }
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci && npm run build
        working-directory: frontend/helpdesk-web
      - run: dotnet publish src/Helpdesk.API/Helpdesk.API.csproj -c Release -o publish
      - run: test -f publish/wwwroot/index.html
